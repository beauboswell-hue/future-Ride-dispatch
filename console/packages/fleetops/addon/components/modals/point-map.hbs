<Modal::Default @modalIsOpened={{@modalIsOpened}} @options={{@options}} @confirm={{@onConfirm}} @decline={{@onDecline}}>
    <div class="fleetbase-leaflet-map-container">
        <LeafletMap class="map-height-base" @lat={{@options.latitude}} @lng={{@options.longitude}} @zoom={{12}} as |layers|>
            <layers.tile @url="https://{s}.basemaps.cartocdn.com/light_all/{z}/{x}/{y}.png?key=cb1_32li_1_aa9e6424da513b1200464fad" />

            <layers.marker @icon={{@options.icon}} @location={{point-to-coordinates @options.location}} @riseOnHover={{true}} as |marker|>
                <marker.popup @maxWidth="500" @minWidth="180">
                    {{#if @options.popupText}}
                        <div>{{@options.popupText}}</div>
                    {{/if}}
                    <div>{{format-point @options.location}}</div>
                </marker.popup>
                {{#if @options.tooltip}}
                    <marker.tooltip @permanent={{false}} @sticky={{true}} @direction="top">{{@options.tooltip}}</marker.tooltip>
                {{/if}}
            </layers.marker>
        </LeafletMap>
    </div>
</Modal::Default>